# Task catalog

Phase 3 provides the task framework but intentionally contains no task baselines.

Phase 4 is reserved for the three required pilots:

- TRN-001 — transient database retry;
- TRN-003 — contract-first by-id endpoint;
- TRN-012 — idempotent order creation.

They are listed here only as planned identifiers. No defect implementation, seed, reproduction, visible/hidden test, baseline branch, or solution is present yet. Catalog expansion remains blocked until all three pilots pass their full reset → reproduce → fix → verify gates.
